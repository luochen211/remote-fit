# SMTP credentials stay local. Never commit a populated .env file.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=you@example.com
SMTP_PASS=replace-with-an-app-password
MAIL_FROM="Your Name <you@example.com>"

