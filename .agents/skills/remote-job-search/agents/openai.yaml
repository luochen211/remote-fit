interface:
  display_name: "Remote Job Search"
  short_description: "Find and evaluate remote jobs open to China"
  default_prompt: "Use $remote-job-search to evaluate whether this remote role is genuinely open to a candidate based in China."
